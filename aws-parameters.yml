- name: Create All RoboShop Parameters
  hosts: localhost
  tasks:
    - name: Create value
      community.aws.ssm_parameters:
        name: "param.dev.catlogue.MONGO"
        value: "false"
        region: "us-east-1"