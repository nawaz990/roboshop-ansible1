- name: Create All RoboShop Parameters
  host: localhost
  tasks:
    - name: Create Value
      community.aws.aws_ssm_paramater_store:
        name: "param.dev.catalogue.MONGO"
        value: "false"